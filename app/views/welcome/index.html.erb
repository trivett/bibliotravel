
<div class="span10" id="landing" class="clearfix">
<div class="span9"> <!-- Hero Unit -->



          <% unless current_user.present? %>
            <div class="hero-unit">
              <h1>Welcome to BiblioTravel!</h1>
                <p> You can search for books that take place in <strong>
                  <%= @city %>
                </strong> or find a good book that takes place in another city.</p>
                <%= link_to 'Sign in with Facebook to get started', '/auth/facebook', id: 'sign_in' %>
          <% end %>

            <% if current_user.present? %>
             <p> Welcome back!</p>
             <p>Here are some books that take place in <strong><%= @city %></strong>:</p>
             <table class='table-striped'>
              <thead>
                <th>Title</th>
                <th>Author</th>
              </thead>
              <tbody>
              <% @books.each do |b| %>
                <tr>
                   <td><%= b.title %></td>
                   <td><%= b.author %></td>
                </tr>
               <% end %>
              </table>
              <p>Here are some authors associated with <strong><%= @city %></strong>:</p>
              
              <p>And here are some famous literary events from <strong><%= @city %></strong></p>
            <% end %>
            <%= paginate @books %>


</div> <!-- Hero Unit -->

